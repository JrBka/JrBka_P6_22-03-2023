<h1>Activez votre compte</h1>
<p>Bonjour {{ user.username }},</p>
<p>Votre inscription sur le site snowtricks est à valider en cliquant sur le lien ci-dessous :</p>
<p><a href="{{ absolute_url(path('check_token', {'slug':'activation',token: token})) }}">{{ absolute_url(path('check_token', {'slug':'activation',token: token})) }}</a></p>
<p>Ce lien expirera dans 1 heure</p>

